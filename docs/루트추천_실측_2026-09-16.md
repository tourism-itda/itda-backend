# 루트 추천 실측과 구현 — 2026-09-16

> 결과 파일(`build/route-probe/*.json`)은 Git 미추적이라 여기에 수치를 남긴다.
> 어제 기록은 `루트추천_인수인계_2026-09-15.md`, `루트추천_표본테스트_2026-09-15.md` 참고.

## 확정된 구성 규칙

| 작품 관련 명소 | 배치 |
| --- | --- |
| 0곳 | 루트를 만들지 않고 숨긴다 |
| 1곳 | 관련 1 + 일반 2 |
| 2곳 이상 | 관련 2 + 일반 1 (3곳 이상 있어도 2곳까지만) |

- 상수는 `RoutePlanner.MAX_RELATED_SPOTS = 2`.
- 방문 순서는 `VisitOrderOptimizer` 가 동선 기준으로 정하고 그 순서 그대로 템플릿 칸에 넣는다.
  관련/일반 여부는 순서에 영향을 주지 않는다. `recommend_order` 는 더 이상 순서 기준이 아니다.

## 소스 역할 분담

관광공사 TourAPI 활용이 프로젝트 전제이므로 아래 순서를 코드에 고정했다.

| 용도 | 1순위 | 2순위 |
| --- | --- | --- |
| 일반 명소 | TourAPI 관광지(12) + 문화시설(14), 5→10→15km | 없음 (TourAPI 전용) |
| 앵커 실재 검증 | TourAPI `searchKeyword2` | 카카오 키워드 검색 |
| 식당·카페 | TourAPI 음식점(39) | 카카오 FD6/CE7 |
| 자동차 경로·소요시간 | 카카오모빌리티 | — |
| 지도 표시 | 카카오맵 SDK (프론트) | — |

TourAPI 의 `mapx`/`mapy` 는 WGS84 라 카카오맵·카카오모빌리티에 좌표 변환 없이 그대로 넣는다.

## 커버리지 실측 (공개 작품 93편)

| 경로 | 편수 |
| --- | ---: |
| 인물 체인(content_person→place_person) | 22 |
| LLM 발굴 드라이런으로 확보 | 28 |
| **합계** | **50 (54%)** |
| 관련 명소 0곳 → 숨김 대상 | 43 |

- 43편 중 41편은 국내에 관련 장소가 실제로 없다(`모가디슈` 소말리아, `안시성` 중국, `봉이 김선달` 평양, SF·순수 창작 사극).
  나머지 2편(`연평해전`, `변호인`)은 검색 키워드를 못 맞춘 경우다.
- 앵커 출처는 TourAPI 30건 / 카카오 11건 — 관광공사 비중 73%.
- 인물 체인 22편 중 앵커가 25km 안에 모이는 것은 8편이고 14편은 흩어져 있다.
  가장 심한 것은 이순신 소재 작품들로 현충사(아산)와 한산도가 265km 떨어져 있다.
  `ContentAnchorResolver.densestCluster` 가 가장 많이 모인 지역만 남긴다.

## TourAPI 주변 검색 커버리지 (앵커 6곳 총건수)

| 앵커 | 관광지+문화시설 5km | 10km | 15km |
| --- | ---: | ---: | ---: |
| 경복궁 (서울 도심) | 227 | 391 | 576 |
| 수원 화성 | 48 | 93 | 162 |
| 화성 융건릉 | 11 | 75 | 118 |
| 세종대왕릉 (여주) | 10 | 23 | 50 |
| 현충사 (아산) | 11 | 39 | 68 |
| 외암민속마을 (농촌) | 11 | 35 | 60 |

가장 열악한 농촌 앵커에서도 5km 에 11곳이라 일반 명소 1~2곳 채우기에 부족하지 않다.

식당·카페는 카카오가 압도적으로 많지만(경복궁 5km 기준 TourAPI 210 vs 카카오 18,145)
구간당 후보는 8개만 필요해서 TourAPI 1순위로도 충분하다.

## 실호출로 발견해 고친 버그 5개

1. **표기 차이로 0건** — `제주4.3평화공원` 은 0건, `제주4·3평화공원`(가운뎃점)은 잡힌다.
   → 검색어 변형(가운뎃점·공백)을 순차 시도하고 contentTypeId 를 12→14→전체로 넓힌다.
2. **부분일치 오탐** — `이화장`(이승만 사저) 검색에 `한국자이화장품`(강서구 화장품 가게)이 걸렸다.
   → 결과 이름이 검색어로 시작하거나 끝나거나 여는 괄호 뒤에 와야 통과.
   TourAPI 는 `서울 운현궁` 처럼 지역을 앞에 붙여 등록하므로 접두만 보면 정상 결과를 버린다.
3. **동명 장소** — `자유공원` 은 안양시 것이, `전쟁기념관` 은 창녕 박진전쟁기념관이 1순위로 나온다.
   → `DiscoveredPlace.region` 을 LLM 에게 받아 결과 주소와 대조한다.
   관광API 주소는 `경상남도`, LLM 은 `경남` 이라 약칭 매핑이 필요하다.
   이름 검사만으로는 `박진전쟁기념관` 을 못 거르므로 지역 검사와 반드시 함께 써야 한다.
4. **카카오 폴백 조건 오류** — "TourAPI 0건일 때만" 폴백해서, 결과는 있으나 전부 검증 탈락한
   경우(`이화장`)에 폴백을 건너뛰었다. → "검증 통과가 없을 때" 로 바꾸니 26편 → 28편.
5. **카카오 키 충돌** — `application.yml` 의 `kakao.client-id` 가 `KAKAO_REST_API_KEY` 를 쓴다.
   지도용 키로 그 변수를 덮으면 카카오 로그인이 다른 앱으로 넘어가 깨진다.
   → 지도·검색·길찾기는 `KAKAO_MAP_REST_KEY` 로 분리했다.

## 카카오 앱 설정

- 로그인 앱(`itda`)은 카카오맵 제품이 꺼져 있어 로컬 API 가
  `403 NotAuthorizedError: App(itda) disabled OPEN_MAP_AND_LOCAL service` 를 돌려준다.
- 카카오맵이 활성화된 별도 앱의 REST 키로 검색·길찾기를 확인했다(모두 HTTP 200).
- **GitHub Secrets 에 `KAKAO_MAP_REST_KEY` 를 등록해야 한다.** 없으면 배포 후 지도 관련
  조회가 예외 없이 빈 결과만 돌려주므로 조용히 실패한다.

## 2026-09-17 운영 DB 적재 결과

운영 DB(EC2 컨테이너)에 SSH 터널로 붙어 실제 수치를 확인하고 앵커를 적재했다.

### 적재 전 실측

| 구분 | 편수 | 원인 |
| --- | ---: | --- |
| 인물 연결 자체가 없음 | 62 | 수집 때 실존 인물 매칭 실패. 인물 DB 95명이 대부분 전근대라 근현대물이 붙지 않는다 |
| 인물은 있으나 그 인물의 장소가 0곳 | 4 | 취화선(장승업), 사도(사도세자), 봉오동 전투(홍범도), 자산어보(정약전) |
| 앵커 확보 | 22 | |

`content_place` 는 0건이었다. 즉 승인 매핑은 한 건도 없었다.

### 적재 후

| 항목 | 값 |
| --- | ---: |
| 공개 작품 | 88 |
| 루트 가능 | **48 (55%)** |
| 숨김 대상 | 40 |
| place | 167 (SEED 121 / TOUR_API 35 / KAKAO 11) |
| content_place | 35행 / 26편 |

앵커 발굴은 Anthropic API 키 없이 진행했다. `AnchorDiscoveryService` 의 LLM 단계를 사람(Claude 세션과 서브에이전트)이 대신하고, 검증·적재는 같은 규칙을 쓰는 스크립트로 처리했다.
남은 40편은 대부분 국내에 관련 장소가 실제로 없다 — 순수 창작 사극(어우동·내시·기방도령), 해외 배경(모가디슈·무사), SF.

### 이날 추가로 발견해 고친 것

1. **앵커가 정확히 2곳일 때 지역 클러스터링을 건너뛰었다.** 명량이 현충사(아산)와 한산도(통영)를
   265km 로 이어 도착 시각이 23:59 로 잘렸다. 이순신·장영실처럼 연고지가 2곳인 인물이 많아
   바로 드러났다. 검사 하한을 1곳으로 내려 수정했고, 명량은 5.1km / 14분이 됐다.
2. **`place_source_check` 가 KAKAO 를 거부한다.** Java enum 에만 KAKAO 를 추가하고 DB 제약은
   그대로였다. `ddl-auto: update` 는 기존 CHECK 제약을 갱신하지 않으므로 새 환경에서 재발한다.
   마이그레이션 도구 도입을 검토할 것. 적용한 구문:
   `ALTER TABLE place ADD CONSTRAINT place_source_check CHECK (source IN ('SEED','TOUR_API','KAKAO'));`
3. **h2 스모크 프로파일로 앱이 뜬 적이 없었다.** AWS SDK 가 빈 문자열 키를 거부해
   S3Config → S3Service → UserController 순으로 기동이 실패했다.

### 남아 있는 품질 문제

- **일반 명소 선정이 거리순뿐이다.** 관광 가치 필터가 없어 '뜻모아센터', '인천업사이클에코센터',
  '신석구 사택 터' 같은 곳이 코스에 들어온다. TourAPI 대표이미지 유무 등으로 거를 수 있다.
- **방문 순서가 운영시간을 보지 않는다.** `VisitOrderOptimizer` 는 동선만 본다.
  하루 시작을 09:00 으로 당겨 완화했으나 근본 해결은 아니다.
- **운영 `JWT_SECRET` 이 빈 값이다.** GitHub Secrets 미등록으로 보인다. 로그인 동작 확인 필요.

### 적재에 쓴 자료

- `scripts/verify-rediscover.py` — 후보를 실제 TourAPI/카카오로 검증
- `scripts/gen-anchor-load-sql.py`, `scripts/gen-rediscover-load-sql.py` — 검증 결과를 SQL 로
- `scripts/load-anchors-tour_api.sql` (16편 23건), `load-anchors-kakao.sql` (8편 9건),
  `load-rediscovered.sql` (4편). 전부 멱등이고 롤백으로 사전 검증했다.

## 남은 작업

1. 운영 DB 접속과 `ANTHROPIC_API_KEY` 확보.
2. `POST /api/admin/route/discover-anchors?only_missing=true&max_contents=5` 로 소량 먼저 실행하고
   결과를 사람이 검수한다. 한 번에 전체를 밀지 않는다.
3. 검수 후 전체 배치 → `POST /api/itineraries/route` 로 실제 루트 확인.
4. 관련 명소 0곳 작품의 숨김 처리를 목록·검색·시대별·인물별·상세 모든 경로에 적용.
5. 프론트에 전달할 응답 변경: `anchor_source`, `travel_distance_meters`, `travel_duration_seconds`,
   `road_verified`, `within_time_limits`, 후보의 `source`, 슬롯의 `GENERAL` 구분.

## 재현 자료

- `scripts/route-coverage-probe.py` — 공개 API 로 작품→인물→장소 커버리지 측정
- `scripts/source-coverage-probe.py` — TourAPI 와 카카오의 주변 검색 건수 비교
- `scripts/discovery-dryrun.py` — 앵커 발굴의 검증 단계를 실제 API 로 시험
- `scripts/route-coverage.sql` — 운영 DB 에서 같은 커버리지를 재는 읽기 전용 질의
- 키는 `.env` 에만 두고 코드·문서·결과 파일에 쓰지 않는다.
